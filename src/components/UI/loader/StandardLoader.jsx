import React from 'react';
import styles from './standard_loader.module.scss'

const StandardLoader = () => {
    return (
        <div className={styles.loader}>
        </div>
    );
};

export default StandardLoader;